# Maps venue abbreviation (the `abbr:` field of a bibtex entry) → url and color.
# Used by jekyll-scholar to render the colored venue badge next to publications.
# Add entries here when you start populating _bibliography/papers.bib.
